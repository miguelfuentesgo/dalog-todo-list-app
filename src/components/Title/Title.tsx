import styles from './Title.module.scss';
export const Title = () => {
    return (
        <section className={styles.title}>
            <h1><span><b>DALOG </b></span> Todo List</h1>
        </section>
    )
}